<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.myapplication">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".focus.Timer"></activity>
        <activity android:name=".focus.focus" />
        <activity android:name=".Search" />
        <activity android:name=".Display" />
        <activity
            android:name=".MainActivity"
            android:label="Diary">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".account.ChartActivity"
            android:label="Chart" />
        <activity android:name=".datepick" />
        <activity android:name=".date_calculate" />
        <activity android:name=".NewEvent" />
        <activity android:name=".SpecialDay" />
        <activity android:name=".EditActivity" />
        <activity android:name=".account.goal_cost" />
        <activity android:name=".account.test_month" />
        <activity android:name=".account.monthlycost" />
        <activity android:name=".account.totalCost" />
        <activity android:name=".account.AccountBookActivity" />
    </application>

</manifest>